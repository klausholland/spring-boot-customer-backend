## Customer Manager

Backend application for managing customers and their address data.

### Setup

To build and run the application run the following command:

```bash
docker compose up -d
```

The application runs on ``http://localhost:8080``

The OpenAPI Specification can be inspected on: `http://localhost:8080/swagger-ui/index.html`